/**
 * Provides various sources of UGA Bus System data to use throughout the backend. Data sources
 * may provide both constant (unchanging data, e.g. what is stored in the database) and dynamic
 * (changing data, e.g. bus information from the API or models) data.
 */
package dataSources;