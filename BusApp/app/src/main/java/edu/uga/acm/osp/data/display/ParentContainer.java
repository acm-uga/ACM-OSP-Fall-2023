package edu.uga.acm.osp.data.display;

/**
 * Outlines the types of containers a {@code DisplayableObject} may be rendered under in the UI that are not existing
 * {@link DisplayableObject}s
 *
 * @see dataDisplay.UiContext
 */
public enum ParentContainer implements DisplayableObject {
    GENERAL
}