package edu.uga.acm.osp.data.sources;

import edu.uga.acm.osp.data.baseClasses.Route;
import edu.uga.acm.osp.data.baseClasses.Stop;
import edu.uga.acm.osp.data.routeSchedule.RouteSchedule;

/**
 * Contains methods that generate example objects with random, but valid data for UI testing.
 */
public class ExampleGenerator {
    /**
     * Instantiates a {@code Route} object with random, logical data for testing purposes.
     *
     * @return a {@code Route} object with random, logical data
     *
     * @see Route
     */
    public static Route getRoute() {

    }

    /**
     * Instantiates a {@code Stop} object with random, logical data for testing purposes.
     *
     * @return a {@code Stop} object with random, logical data
     *
     * @see Stop
     */
    public static Stop getStop() {

    }

    /**
     * Instantiates an array of {@code Stop} objects with random, logical data for testing purposes.
     *
     * @return an array of {@code Stop} objects with random, logical data
     *
     * @see Stop
     */
    public static Stop[] getStops() {

    }

    /**
     * Instantiates an array of {@code Route} objects with random, logical data for testing purposes.
     *
     * @return an array of {@code Route} objects with random, logical data
     *
     * @see Route
     */
    public static Route[] getRoutes() {

    }

    /**
     * Instantiates a {@code RouteSchedule} object with random, but logical data for testing purposes.
     *
     * @return a {@code RouteSchedule} object with random, but logical data for testing purposes
     *
     * @see RouteSchedule
     */
    private static RouteSchedule getRouteSchedule() {

    }
}
